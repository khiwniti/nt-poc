[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3

[[healthcheck]]
path = "/api/v1/health"
interval = 30
timeout = 10
