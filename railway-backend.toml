[build]
builder = "dockerfile"
dockerfilePath = "services/backend/Dockerfile"
watchPaths = ["services/backend/**"]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[healthcheck]]
path = "/api/v1/health"
interval = 30
timeout = 10